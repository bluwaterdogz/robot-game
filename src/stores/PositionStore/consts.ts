import { BoardDimension, Position } from './types';

export const InitialBoardDimensions: BoardDimension = {
  x: 5,
  y: 5,
};

export const StartingPosition: Position = {
  x: 0,
  y: 0,
};

export const InitialCellSize = 65;
